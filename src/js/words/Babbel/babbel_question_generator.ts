import {Question} from "../CustomVocab/types";
import BabbelVocabEntry from "./babbel_vocab_entry";

export default class BabbelQuestionGenerator {

    static getQuestions(item: BabbelVocabEntry) {
        const q: Question[] = [];

        return q;
    }

}
